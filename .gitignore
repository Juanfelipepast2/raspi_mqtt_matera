# Secretos: nunca subir el .env con credenciales reales.
.env

# Entorno virtual de Python.
venv/
__pycache__/
*.pyc

# Histórico de sensores (SQLite) generado en tiempo de ejecución.
history.db
history.db-journal
